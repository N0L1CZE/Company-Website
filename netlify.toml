[build]
  command = "npm ci && npx prisma generate && PRISMA_MIGRATE_SKIP_LOCKS=true npx prisma migrate deploy && npm run build"
  publish = ".next"

[build.environment]
  # pro build
  ADMIN_EMAIL         = "admin@admin.cz"
  ADMIN_PASSWORD_HASH = "$ADMIN_PASSWORD_HASH"
  JWT_SECRET          = "$JWT_SECRET"

[functions.environment]
  # pro runtime vašich API‐routes
  CLOUDINARY_URL      = "cloudinary://721197318981415:cbd8qq_a-S18FWph0cFMnYukW-0@drmdedjur"
