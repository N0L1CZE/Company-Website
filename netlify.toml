[build]
  publish   = ".next"
  command   = "npm ci && npx prisma generate && npm run seed && npm run build"

[build.environment]
  DATABASE_URL          = "postgresql://neondb_owner:npg_3XaCIKSHM9it@ep-cold-flower-a575rvtu-pooler.us-east-2.aws.neon.tech/neondb?sslmode=require"
  ADMIN_EMAIL           = "admin@admin.cz"
  ADMIN_PASSWORD_HASH   = "$ADMIN_PASSWORD_HASH"   # Netlify UI injectne hodnotu
  JWT_SECRET            = "$JWT_SECRET"

[functions.environment]
  # funkční váhy pro serverless (pokud je potřebujete):
  DATABASE_URL          = "$DATABASE_URL"
  ADMIN_EMAIL           = "$ADMIN_EMAIL"
  ADMIN_PASSWORD_HASH   = "$ADMIN_PASSWORD_HASH"
  JWT_SECRET            = "$JWT_SECRET"
